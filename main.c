int main() {
	int i;
	int j;
	int a, b, c , d; // user inputs
	int g, h; // led outputs

	for (i = a; i < a+104; i++) {
		for (j = 3; j < 104; j++) {
			if(is_odd_parity(a)) {
				g = exp(c, b);
				g = pop_count(g+b);
				h = pop_count(c+a);	
			}
		}
	}
}